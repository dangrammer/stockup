const initialState = {
  stocks: {},
  errors: []
}


const stockReducer = (state = initialState, action) => {
  switch (action.type) {

    default:
     return state
  }
}  

export default stockReducer